import type { TodoStatus, TodoPriority } from "@/lib/types"

const todoStatusOptions: TodoStatus[] = ["open", "close", "in-progress", "pending"]
const todoPriorityOptions: TodoPriority[] = ["low", "medium", "high"]
