import type { TodoStatus, TodoPriority } from "@/lib/types"

export const todoStatusOptions: TodoStatus[] = ["open", "close", "in-progress", "pending"]
export const todoPriorityOptions: TodoPriority[] = ["low", "medium", "high"]
